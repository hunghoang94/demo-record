import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  contact: {
    height: 40,
    alignItems: "center",
    backgroundColor: "#DDDDDD",
    padding: 10,
    marginBottom: 20
  }
});
